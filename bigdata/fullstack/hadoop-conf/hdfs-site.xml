<!-- etc/hadoop/hdfs-site.xml -->
<configuration>
    <property>
        <name>dfs.replication</name>
        <value>1</value>
    </property>

    <!-- for NameNode only -->
    <property>
        <name>dfs.namenode.name.dir</name>
        <value>file:/hadoopfs/hdfs/namenode</value>
    </property>

    <!-- for DataNode only -->
    <property>
        <name>dfs.datanode.data.dir</name>
        <value>file:/hadoopfs/hdfs/datanode</value>
    </property>
</configuration>
